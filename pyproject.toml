[project]
name = "firefox-l10n-compare"
version = "0.1.0"
description = "Firefox L10n completeness dashboard generator"
requires-python = ">=3.11"
dependencies = [
    "plotly>=5.0",
    "jinja2>=3.0",
]

[project.scripts]
compare = "compare:main"
